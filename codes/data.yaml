# STRIDE-Net dataset descriptor (template)
# Actual nursery imagery is available on reasonable request (see README).

path: ./yolo_dataset      # dataset root directory
train: train/images        # training images (relative to path)
val: val/images            # validation images (relative to path)
test: test/images          # test images (relative to path)

# Classes
names:
  0: spacing
